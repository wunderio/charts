{{- if .Values.ssl.enabled }}
{{- include "silta-cluster.cert-manager-api-version" . }}
kind: ClusterIssuer
metadata:
  name: selfsigned
  namespace: {{ .Release.Namespace }}
spec:
  selfSigned: {}
{{- end }}
